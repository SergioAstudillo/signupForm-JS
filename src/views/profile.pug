doctype html
html(lang="en")
    head
        title User Profile.
        link(rel="stylesheet" type="text/css" href="public/css/profile.css")
        include head.pug
    body
        //WIP (Dummy text)
        .card__container
            .card
                h1.card__title Here is your profile information:
                .card__paragraph--wrapper
                    p.card__paragraph This is your email:
                    span.card__data #{email}
                    p.card__paragraph This is your fullname:
                    span.card__data.card__data--fullname #{fullname}
                    p.card__paragraph This is your password:
                    span.card__data #{password}
                    p.card__paragraph You created your profile:
                    span.card__data #{created_at}
                .button.form__submitButton--container 
                    //TODO: MAKE THIS BUTTONS FUNCTIONAL WITH SQL AND JS.
                    button.form__submitButton--left(type="button" onclick="") Change data
                    button.form__submitButton--right(type="button" onclick="") Log out
            script(src="https://cdn.jsdelivr.net/npm/sweetalert2@9")